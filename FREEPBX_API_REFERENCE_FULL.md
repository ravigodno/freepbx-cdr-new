# FREEPBX_API_REFERENCE.md

> PBXPuls Internal FreePBX Integration Reference

Version: 1.0 (Living Document)

## Purpose

This document contains **only verified information** about the FreePBX
installation used by PBXPuls.

Before implementing any FreePBX feature:

1.  Read this document.
2.  Read PBXPULS_ARCHITECTURE.md.
3.  Search existing PBXPuls code.
4.  Search `/opt/freepbx-api-dashboard`.
5.  Reuse existing helpers.

Never guess endpoints.

------------------------------------------------------------------------

# Current Environment

REST Base

`http://127.0.0.1:83/admin/api/api/rest`

OAuth Token

`http://127.0.0.1:83/admin/api/api/token`

GraphQL

`http://127.0.0.1:83/admin/api/api/gql`

OAuth:

-   client_credentials
-   application/x-www-form-urlencoded
-   Bearer access_token

Node compatibility:

-   Node 16
-   fetch polyfill compatibility required

------------------------------------------------------------------------

# VERIFIED REST ENDPOINTS

## GET /core/users

Status: VERIFIED

Primary source of extensions.

Returns:

-   extension
-   name
-   voicemail

Display Name MUST come from this endpoint.

------------------------------------------------------------------------

## GET /userman/extensions

Status: VERIFIED

Purpose:

Userman mapping only.

Returns:

-   id
-   username
-   description

Rules:

description MUST NEVER become displayName.

id MUST NEVER become extension number.

Use only:

-   username
-   usermanId

------------------------------------------------------------------------

## GET /extensions

Current PBX:

404 Not Found

Never use.

------------------------------------------------------------------------

# VERIFIED MERGE ORDER

Identity:

1.  /core/users

Enrichment:

2.  /userman/extensions

Devices:

3.  GraphQL

Advanced:

4.  GraphQL

Fallback:

5.  MariaDB only if REST/GraphQL cannot provide data.

------------------------------------------------------------------------

# VERIFIED EXPECTED EXTENSIONS

100 200 201

Display names:

100 → 100

200 → Грунин Константин

201 → User 201

If UI shows:

1 2 3

merge is broken.

If UI shows:

Autogenerated user on new device creation

displayName is broken.

------------------------------------------------------------------------

# VERIFIED UI RULES

Never display:

/userman/extensions.description

as extension name.

Unknown technical data:

Technology: Не определено

Recording: Нет данных

These are informational states.

Do NOT mark extension as Warning because technical enrichment is
unavailable.

------------------------------------------------------------------------

# VERIFIED RAW DEBUG SCREEN

Raw REST page exists only for diagnostics.

Requirements:

-   collapsible
-   close button
-   secrets masked
-   never used by business logic

------------------------------------------------------------------------

# BULK HANDLER PARITY

Target parity fields:

Identity

-   extension
-   password (masked)
-   name
-   voicemail

Device

-   id
-   tech
-   dial
-   devicetype
-   user
-   emergency_cid

SIP/PJSIP

-   context
-   callerid
-   transport
-   secret (masked)
-   allow/disallow
-   qualify
-   nat
-   dtmfmode
-   host

Recording

-   inbound external
-   outbound external
-   inbound internal
-   outbound internal
-   ondemand
-   priority

Call Waiting

-   enable
-   cwtone

Find Me Follow

-   strategy
-   group list
-   destination
-   enabled

CID

-   outbound CID
-   emergency CID

------------------------------------------------------------------------

# GRAPHQL POLICY

Preferred enrichment source:

/opt/freepbx-api-dashboard

Reuse existing queries:

-   fetchAllCoreDevices
-   fetchAllExtensions

Never rewrite GraphQL logic if working code already exists.

------------------------------------------------------------------------

# NORMALIZED EXTENSION MODEL

Minimum model:

-   extension
-   displayName
-   voicemail
-   usermanId
-   username
-   tech
-   dial
-   deviceType
-   recording
-   callWaiting
-   outboundCid
-   emergencyCid
-   email
-   raw
-   sourceStatus

raw should keep source sections separately.

------------------------------------------------------------------------

# SINGLE EDIT UX

Row click:

Open right side editor.

Checkbox:

Bulk selection only.

Workflow:

Edit → Preview → Apply (future)

Single preview uses:

POST /api/management/extensions/update-preview

selectedExtensions = \[extension\]

------------------------------------------------------------------------

# BULK CREATE

Modes:

-   range
-   manual
-   csv

Always:

Preview first.

------------------------------------------------------------------------

# BULK UPDATE

User explicitly selects fields.

Unchecked fields must never change.

------------------------------------------------------------------------

# APPLY SAFETY

Never automatically call:

-   create-apply
-   update-apply
-   fwconsole reload

without explicit confirmation.

------------------------------------------------------------------------

# SECURITY

Never expose:

-   password
-   secret
-   JWT
-   OAuth token
-   API key

Always mask:

------------------------------------------------------------------------

------------------------------------------------------------------------

# DEVELOPMENT CHECKLIST

Before coding:

✓ Read AGENTS.md

✓ Read PBXPULS_ARCHITECTURE.md

✓ Read this file

✓ Search PBXPuls

✓ Search freepbx-api-dashboard

✓ Reuse helpers

✓ Build

✓ Show git diff --stat

Never reinvent FreePBX API.


## Extensions Data Source Compatibility Matrix

PBXPuls must not rely on a single FreePBX Extensions source. Use the shared provider chain:

1. BMO local.
2. GraphQL API.
3. Database readonly.
4. Legacy REST fallback.

Compatibility:

- FreePBX 17 / Debian 12: GraphQL API preferred, BMO/DB fallback when available.
- FreePBX 16 / Sangoma/CentOS 7: BMO local preferred, DB fallback.
- FreePBX 15: BMO local preferred, DB fallback.
- Remote PBX: GraphQL preferred, DB only if MySQL readonly access is explicitly available.

Legacy REST endpoints /userman/extensions and /core/users may return ajaxRequest declined on FreePBX 17 and must be used only as last fallback. Do not implement Extensions as GraphQL-only because older verified schemas may not expose fetchAllExtensions.
