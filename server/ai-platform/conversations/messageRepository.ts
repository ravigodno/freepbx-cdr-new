import type { AiPlatformStore } from '../storage/aiPlatformStore.js';
export class MessageRepository{constructor(private store:AiPlatformStore){}async add(tenantId:number,conversationId:number,role:string,content:string,extra:any={}){const rows=await this.store.query('SELECT COALESCE(MAX(sequence_no),0)+1 next_no FROM ai_conversation_messages WHERE conversation_id=? AND tenant_id=?',[conversationId,tenantId]);await this.store.query('INSERT INTO ai_conversation_messages (tenant_id,conversation_id,sequence_no,role,content,content_json,provider_message_id,token_json,latency_ms) VALUES (?,?,?,?,?,?,?,?,?)',[tenantId,conversationId,Number(rows[0]?.next_no||1),role,content.slice(0,8000),'{}',extra.providerRequestId||null,JSON.stringify(extra.usage||{}),extra.latencyMs??null])}}
