import type { RealtimeVoiceProviderAdapter } from './realtimeVoiceProviderAdapter.js';
import { RealtimeVoiceError } from './realtimeVoiceErrors.js';
export class RealtimeVoiceProviderRegistry { private readonly adapters=new Map<string,()=>RealtimeVoiceProviderAdapter>();register(key:string,factory:()=>RealtimeVoiceProviderAdapter){if(!/^[a-z0-9_-]{2,64}$/.test(key)||this.adapters.has(key))throw new RealtimeVoiceError('conflict',409,'Invalid or duplicate realtime provider');this.adapters.set(key,factory)}create(key:string){const factory=this.adapters.get(String(key||'').toLowerCase());if(!factory)throw new RealtimeVoiceError('provider_unknown',404,'Unknown realtime voice provider');return factory()}list(){return[...this.adapters.entries()].map(([key,factory])=>({key,capabilities:factory().getCapabilities()}))}}
