import type{BusinessActionDefinition}from'./actionTypes.js';import{BusinessActionError}from'./actionErrors.js';
const callback:BusinessActionDefinition={key:'business.create_callback_request',version:1,name:'Создание заявки на обратный звонок',description:'Создать подтверждённую заявку, чтобы сотрудник компании перезвонил клиенту',riskLevel:'low',executorKey:'create_callback_request',defaultApprovalMode:'customer_consent',allowedAutonomyLevels:['SAFE','ASSISTED','AUTONOMOUS'],idempotent:true,timeoutMs:5000,enabled:true,inputSchema:{type:'object',required:['phone','reason'],additionalProperties:false,properties:{contactName:{type:'string',maxLength:191},phone:{type:'string',minLength:5,maxLength:32},reason:{type:'string',minLength:2,maxLength:1000},preferredTime:{type:'string',maxLength:191},timezone:{type:'string',maxLength:64},priority:{type:'string',enum:['normal','urgent']}}},outputSchema:{type:'object',required:['callbackRequestId','safeSummary','duplicate'],additionalProperties:false,properties:{callbackRequestId:{type:'integer',minimum:1},safeSummary:{type:'string',minLength:1,maxLength:500},duplicate:{type:'boolean'}}}};
export class ActionDefinitionRegistry{private readonly definitions=new Map([[callback.key,callback]]);get(key:string){const item=this.definitions.get(key);if(!item)throw new BusinessActionError('not_found',404,'Unknown business action');return item}list(){return[...this.definitions.values()]}}
let singleton:ActionDefinitionRegistry|null=null;export const getActionDefinitionRegistry=()=>singleton||(singleton=new ActionDefinitionRegistry());
