import React, { useEffect, useMemo, useState } from 'react';
import { BookOpen, GraduationCap } from 'lucide-react';
interface Props { token:string;agentId:number;enabled:boolean;canViewKnowledge:boolean;canViewTraining:boolean }
export default function AgentKnowledgeTrainingPage({token,agentId,enabled,canViewKnowledge,canViewTraining}:Props){const [tab,setTab]=useState<'knowledge'|'training'>('knowledge'),[rows,setRows]=useState<any[]>([]),[error,setError]=useState('');const headers=useMemo(()=>({Authorization:`Bearer ${token}`}),[token]);useEffect(()=>{if(!enabled)return;const allowed=tab==='knowledge'?canViewKnowledge:canViewTraining;if(!allowed){setRows([]);setError('Недостаточно прав');return}setError('');void fetch(`/api/ai-platform/${tab}?agentId=${agentId}`,{headers}).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.error||'Ошибка загрузки');setRows(body.rows||[])}).catch(reason=>setError(reason.message))},[agentId,canViewKnowledge,canViewTraining,enabled,headers,tab]);return <div className="space-y-4"><div className="flex gap-2"><button onClick={()=>setTab('knowledge')} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-bold ${tab==='knowledge'?'bg-blue-600 text-white':'border bg-white'}`}><BookOpen className="h-4 w-4"/>Knowledge</button><button onClick={()=>setTab('training')} className={`flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-bold ${tab==='training'?'bg-blue-600 text-white':'border bg-white'}`}><GraduationCap className="h-4 w-4"/>Training</button></div>{error&&<div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">{error}</div>}<div className="overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-3">Название</th><th className="p-3">Тип</th><th className="p-3">Статус</th><th className="p-3">Версия / дата</th></tr></thead><tbody>{rows.map(row=><tr key={row.id} className="border-t"><td className="p-3 font-semibold">{row.name||row.title}</td><td className="p-3">{row.type}</td><td className="p-3">{row.status}</td><td className="p-3">{row.latest_version||row.created_at||'—'}</td></tr>)}{!rows.length&&!error&&<tr><td colSpan={4} className="p-8 text-center text-slate-500">Нет данных. Runtime и автоматическое обучение выключены.</td></tr>}</tbody></table></div></div>}
